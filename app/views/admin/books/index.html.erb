<% provide :title, t(".books_title") %>

<div class="well well-small"><h1><%= t ".books" %></h1></div>

<div class="row heigh_table_with_button">
  <div class="col-md-2">
    <%= link_to "#", class: "btn btn-primary",
      data: {toggle: "modal", target: "#modal_create_book"} do %>
      <span><i class="fa fa-plus-square" aria-hidden="true"></i></span>
      <%= t ".new_book_button" %>
    <% end %>
  </div>
  <div class="col-md-2">
    <button class="btn btn-default btn-space">
      <%= link_to admin_export_book_excel_index_path(params.merge(format: :xls).permit!) do %>
        <span><i class="fa fa-download" aria-hidden="true"></i></span>
        <%= t ".export_to_excel" %>
      <% end %>
    </button>
  </div>
  <div class="col-md-8">
    <%= form_tag admin_books_path, method: :get do %>
      <div id="custom-search-input">
        <div class="input-group col-md-12">
          <%= text_field_tag :search, params[:search], class: "form-control",
            placeholder: t(".input_books_name") %>
          <span class="input-group-btn">
            <%= button_tag type: "submit", class: "btn btn-default" do %>
              <span class="glyphicon glyphicon-search"></span>
            <% end %>
          </span>
        </div>
      </div>
    <% end  %>
  </div>
</div>

<%= render "table_data" %>

<div class="modal book-modal-edit">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <h4 class="modal-title" id="modal_edit_boo_label">
          <%= t ".update_boo_title" %>
        </h4>
      </div>
      <div class="modal-body-book-edit">

      </div>
    </div>
  </div>
</div>
