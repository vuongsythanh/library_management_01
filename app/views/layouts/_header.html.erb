<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to t(".app_name"), root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t(".home"), root_path %></li>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <i class="fa fa-language"></i> <%= t(".languages") %><b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li>
              <%= link_to_unless I18n.locale == :vn, t(".vietnam"), locale: :vn %>
            </li>
            <li class="divider"></li>
            <li>
              <%= link_to_unless I18n.locale == :en, t(".english"), locale: :en %>
            </li>
          </ul>
        </li>
        <% if logged_in? %>
          <li><%= link_to t(".all_member"), users_url %></li>
          <% if current_user.is_admin? %>
            <li><%= link_to t(".dashboard"), admin_root_url %></li>
          <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t".account" %><b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t(".Profile"), current_user %></li>
              <li class="divider"></li>
              <li>
                <%= link_to t(".settings"), "#",
                  data: {toggle: "modal", target: "#settingsModal"} %>
              </li>
                  <li class="divider"></li>
              <li>
                <%= link_to t(".Logout"), logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <ul class="nav navbar-nav navbar-right">
            <li>
              <%= link_to t(".login"), "#",
                data: {toggle: "modal", target: "#loginModal"} %>
            </li>
          </ul>
        <% end %>
      </ul>
    </nav>
  </div>
</nav>
<%= render "users/modal" %>
<%= render "users/edit" %>
